spring:
  main:
    web-application-type: REACTIVE
  cloud:
    function:
      definition: helloWorldFunction
      web:
        export:
          debug: true

logging:
  level:
    org:
      springframework:
        cloud:
          function: DEBUG